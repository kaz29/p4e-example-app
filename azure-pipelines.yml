
name: p4e-example-app

trigger:
  - master

variables:
  - group: p4e-example-app

jobs:
  - job:
    displayName: UnitTest
    pool:
      vmImage: 'Ubuntu-16.04'
    steps:
      - templates/unittest-steps.yml

  - job: Build
    pool:
      vmImage: 'Ubuntu-16.04'
    variables:
      - group: p4e-example-app
    steps:
      - templates/buils-steps.yml
